///check_flood_stage + flood stage tests
/// notification dispatch, alert deduplication, and cooldown logic will also likely live here, since they're closely related to the concept of a "threshold breach" and may require access to the same metadata about each site (e.g. which parameters have thresholds, what are the threshold values, etc).
/// 